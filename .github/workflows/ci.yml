# Retired placeholder. Maintainers run CI outside this workflow.

name: CI (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::This workflow is retired. Maintainers run CI outside this workflow."
          exit 1
