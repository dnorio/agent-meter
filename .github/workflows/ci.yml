# Aposentado (T-365): CI roda no Jenkins external CI — status jenkins/agent-meter
# Emergência manual apenas. Ver Jenkinsfile + docs/ci-zero-github-actions.md (monorepo).

name: CI (retired — use Jenkins)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Este workflow foi aposentado. CI = Jenkins external CI (jenkins/agent-meter)."
          echo "https://jenkins.ci.example.com/job/agent-meter-oss/"
          exit 1
