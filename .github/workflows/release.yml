# Aposentado (T-365): release roda no Jenkins external CI (Fase 3 T-365)
# Emergência manual apenas.

name: Release (retired — use Jenkins)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.1.1)'
        required: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Release via GHA aposentado. Use Jenkins job agent-meter-oss-release (T-365 Fase 3)."
          echo "Tag solicitada: ${{ github.event.inputs.tag }}"
          exit 1
