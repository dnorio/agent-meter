# Retired placeholder. Maintainers run releases outside this workflow.

name: Release (retired)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.1.1)'
        required: true

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Release via GitHub Actions is retired. Maintainers run release automation externally."
          echo "Tag solicitada: ${{ github.event.inputs.tag }}"
          exit 1
