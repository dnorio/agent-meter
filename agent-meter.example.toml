# agent-meter configuration
# All values can be overridden by environment variables.
# Env vars take precedence over this file.

[server]
host = "127.0.0.1"
port = 8081
otlp_port = 4318

[database]
# SQLite (default — zero-config, single file):
url = "sqlite://agent-meter.db"
#
# PostgreSQL (optional — shared/server deployments):
# url = "postgres://agent_meter:agent_meter@localhost:5432/agent_meter"

[telemetry]
log_level = "info"
# otel_endpoint = "http://localhost:4317"
service_name = "agent-meter"
