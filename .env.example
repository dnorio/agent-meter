# Local development — copy to .env and adjust (never commit .env)
AGENT_METER_HOST=127.0.0.1
AGENT_METER_PORT=8081
# Default OSS backend (zero config):
DATABASE_URL=sqlite://agent-meter.db
# Optional shared backend (not required for OSS):
# DATABASE_URL=postgres://agent_meter:CHANGE_ME@localhost:5432/agent_meter
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=agent-meter
RUST_LOG=info
